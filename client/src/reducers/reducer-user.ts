import {Reducer} from "redux";
import {AllActionTypes} from "../actions/base-action";
import {secureUserInfo} from "../../../src/shared/utils";

export default ((state, action) => {
    switch (action.type) {

    }
}) as Reducer<ReturnType<typeof secureUserInfo>, AllActionTypes>;