import {IUser} from "../../../src/models/User";
import {Reducer} from "redux";
import {AllActionTypes} from "../actions/base-action";

export default ((state, action) => {
    switch (action.type) {

    }
}) as Reducer<IUser, AllActionTypes>;